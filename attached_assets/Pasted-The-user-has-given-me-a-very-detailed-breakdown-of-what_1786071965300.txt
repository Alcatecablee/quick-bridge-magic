The user has given me a very detailed breakdown of what to fix, organized by priority. Let me execute all of these systematically. Let me plan the changes:

P0 (Must fix):

Hero onScrollToQR - Add prop to HeroSection, wire it to a scroll-to-pair CTA
Remove dead code - videoSectionRef, videoReady, scanCount, canInstall, installed, handleInstall, unused imports (logos, Accordion, PhoneIllustration, etc.)
WorkflowComparison double animation - Remove whileInView from inside WorkflowComparison, let Reveal own it
FAQ accessibility - Add id and aria-controls attributes to buttons and panels to properly link them
Fix the broken glow CSS in WorkflowComparison
P1 (Should fix): 6. Hero opacity hierarchy - Ensure all text lines reach 100% opacity after their animations complete 7. Sticky scroll bounce - Disable the infinite animation when the element isn't visible 8. Mobile Security labels - Make all 4 labels visible on mobile by stacking them vertically or allowing them to wrap 9. FAQ expand affordance - Add a subtle plus/minus indicator to show expand state 10. DevicesPanel gradient - Bump the opacity value up slightly for better visibility 11. Reduced motion fallback - Strip out card borders from the ReducedMotionF component

P2 (Polish): 12. Use motion.h1 in HeroSection 13. Hide the activity chip when reduced motion is enabled 14. Workflow eyebrow label styling 15. Clean up unused scenePath and scenePathStyle variables

I'm also thinking about the user's suggestion to make the hero feel more cinematic like an Apple keynote—lots of breathing room and strategic pauses. I'll tackle the P0 items first, then P1, then P2, working through them in parallel where I can. Starting with HeroSection.tsx, I need to add the onScrollToQR prop, switch to motion.h1, fix the opacity hierarchy, handle the reduced-motion chip, and restructure it with that cinematic feel in mind.

For index.tsx, I'm removing a bunch of dead code—the videoSectionRef, videoReady state, the video lazy-loading effect, scanCount state and its Supabase query, canInstall and installed states, the handleInstall function, plus unused imports like the logo constants and various illustration components.

Now moving to WorkflowComparison.tsx to strip out the whileInView animation and simplify it.

Fixing the glow CSS in that component, then tackling FaqSection.tsx by adding proper accessibility attributes to the buttons and panels with an expand affordance.

SecuritySection.tsx needs mobile label fixes for vertical alignment, and StickyScrollSection.tsx should drop the infinite bounce animation and remove unused scene path variables while fixing the reduced-motion fallback.

HeroDeviceViz.tsx needs to handle reduced-motion for the chip state, and DevicesPanel.tsx gets a boost to the gradient opacity.

Now I'm restructuring the hero text to feel more cinematic—changing the motion.div with role="heading" to motion.h1, adding the onScrollToQR prop, and making line2 and line3 animate to full opacity instead of 70% so the reveal feels more impactful.

For the hero layout itself, I'm breaking it into more dramatic chunks: "Move files" and "instantly." as separate massive lines with longer delays between them, followed by the subtitle "Make your devices work together." and then the visualization fades in.

For WorkflowComparison, I need to remove the internal whileInView animations and let the Reveal component handle the entrance timing, plus fix the glow shadow by replacing the Tailwind shadow utility with a proper oklch-based or hardcoded primary color shadow.

Now I'm planning the execution order: starting with index.tsx for dead code removal and prop fixes, then tackling the SecuritySection mobile layout to stack vertically on mobile and horizontally on larger screens, adding a +/× indicator to the FAQ section for better affordance, and fixing the glow effects throughout.

I'll work through each file systematically—HeroSection needs the cinematic restructure with motion.h1 and onScrollToQR, HeroDeviceViz needs the reduced-motion chip fix, WorkflowComparison needs whileInView removed and glow corrected, FaqSection needs accessibility improvements and the affordance indicator, SecuritySection needs mobile labels and alignment fixes, StickyScrollSection needs the bounce fix and scenePath removal, and DevicesPanel needs gradient opacity adjustments.

Let me start removing the dead code from index.tsx—the unused logo imports, FlowIllustrations import, and Accordion imports. I'll wire up the onScrollToQR prop to HeroSection so it actually scrolls to the Pair section when triggered. Now I'm reviewing which state variables are actually still being used after removing the dead code—sessionId, scanning, origin, waitingPing, canShare, pin, and resumeTarget are all needed, but canInstall and installed can be deleted since the install button is gone.

I also need to clean up the unused refs like videoSectionRef and videoReady, remove the handleInstall function that's never called, and delete the logo imports and PhoneIllust that aren't being rendered anywhere. None of these components are actually used in the remaining code, so I can safely remove them. The logo constants and Accordion imports can go too since they were only part of the old sections. Now I'm planning out the specific changes needed for index.tsx - removing the unused imports, state variables, and constants in a series of targeted edits.

For HeroSection, I need to refactor it to accept a scroll callback prop, switch to semantic heading markup, ensure all animations reach full opacity, and add a subtle affordance that hints users can scroll to the QR pairing section. The key is the timing and spacing—I should break "Move files instantly" across separate lines with dramatic pauses between them, letting each phrase land before the next one appears, all at full opacity. Then after a longer delay, introduce the subtitle about devices working together. It's about the cinematic rhythm rather than opacity changes.

Now I'm going through the code to identify exactly where these changes need to happen—the accordion imports, logo declarations, and the illustration component sections—so I can update them all at once. Now I'm planning out the refactoring work across all the components—I'll tackle the dead code removal in index.tsx first, then move through HeroSection with the cinematic restructure and prop fixes, followed by updates to HeroDeviceViz, WorkflowComparison, FaqSection, SecuritySection, StickyScrollSection, and the remaining components. Organizing the file modifications into batches since I can only modify one file at a time. I'll start with the independent components in parallel, then handle the dependent ones, and finish with index.tsx cleanup. Now I'm working through the actual changes needed for each file, starting with HeroSection.tsx where I need to add the scroll callback prop, switch to semantic heading markup with motion, ensure all text lines animate to full opacity without permanent dimming, and increase the timing delays for a more cinematic feel.

Writing HeroSection component... Writing component markup... Writing the CTA button...

Actually, I'm reconsidering the button approach. The user wants something subtle—not a prominent "Get Started" CTA, but rather a quiet affordance like "Start a bridge ↓" that gently guides toward the pairing section below without being pushy.

For the scroll behavior, the onScrollToQR prop is always passed to HeroSection and targets the #qr element. For new users it scrolls to the QR section, while returning users see the DevicesPanel first and the scroll would take them past it. Either way, the behavior works fine.

Now I'm thinking about the cinematic restructuring the user suggested—breaking up the messaging more dramatically. I'll start with the first line being "Move files instantly."

—that lands with a clip reveal, then add a substantial pause (around 0.6 seconds instead of the current 0.3), and then reveal the next two lines at full opacity. This creates that keynote effect where each thought has space to breathe before the next one appears. I also need to make sure all text is at full opacity rather than the dimmed /70 version.

The key is restructuring the timing so "Move files instantly." stands alone as its own moment, then after a pause, the longer thought about making every device feel the same computer comes in as a second beat. This requires adjusting the animation delays significantly to create those huge pauses the user mentioned.

Let me focus on fixing the opacity issue first—all three lines should be at text-foreground at full strength—and then implement the cinematic timing with longer delays between each line reveal.

For the flowComparison.tsx file, I need to remove the whileInView animation since Reveal is already handling the entrance, and simplify the glow effect by using a filter property instead of the shadow utility that's causing issues. The travelling dot animation in the QuickBridge section needs to be adjusted...

Writing component fixes... Fixing the animation triggers...

For the slowBarVariants, they'll still work correctly as children of the stagger container. Now I need to handle the reduced-motion case in HeroDeviceViz—when prefersReduced is true, the activity chip should either not render or show a static neutral label instead of staying frozen on the first activity.

Writing component fixes... I'm organizing the file updates into parallel batches—starting with the independent component rewrites like HeroSection, WorkflowComparison, FaqSection, SecuritySection, and DevicesPanel, then moving to the dependent ones like StickyScrollSection and HeroDeviceViz, and finally cleaning up the index file once the prop interfaces are finalized. I'll do a complete rewrite of HeroSection since the changes are substantial, then use targeted replacements for the others.

For HeroSection I'm adding the onScrollToQR callback prop, animating the first line with motion.h1, ensuring all text reaches full opacity after the animation completes, extending the delays for a more cinematic effect, and including a subtle "Start a bridge ↓" affordance cue. WorkflowComparison needs the whileInView removed and replaced with animate="visible", the glow shadow fixed on the travelling dot, and an eyebrow label added. FaqSection requires accessibility improvements with id and aria-controls attributes on buttons, ids on answer panels, and a minimal expand indicator. SecuritySection needs the mobile flow labels to display vertically instead of being hidden on smaller screens.

For the remaining components, I'm adjusting the gradient opacity in DevicesPanel, removing unused scenePath constants from StickyScrollSection while conditionally rendering the bouncing dot and fixing the reduced-motion fallback, hiding the chip in HeroDeviceViz when reduced motion is preferred, and cleaning up dead code imports from index.tsx including logo constants, FlowIllustrations, and Accordion imports.

I also need to remove several unused state variables and effects from index.tsx—scanCount state, canInstall/installed state, their associated useEffects, the handleInstall function, and videoSectionRef/videoReady. Then I should verify whether StickyScanCta is still being imported since we removed its usage.

I notice HeroSection is receiving an onScrollToQR prop but the component doesn't currently accept it, so I need to add that interface and wire it up. For SecuritySection on mobile, the current hidden sm:inline approach completely hides certain elements, so I should explore stacking them vertically instead to maintain visibility across all screen sizes. 326px should be enough space for all four labels at around 11px font size with proper spacing, so I can remove the hidden breakpoint classes and let them display naturally. Now I'm ready to write out the complete implementations for all the files, starting with addressing the scroll hint bounce issue in the StickyScrollSection component. The issue is that the inner bounce animation keeps running even when the outer container fades out. I should conditionally render the entire scroll indicator using AnimatePresence so it unmounts completely when activeIndex is greater than zero, rather than just hiding it with opacity. I'm also checking how the reduced-motion fallback is currently implemented.

Now I need to update the styling for the card components to use a divider aesthetic with bottom borders instead of the rounded card style, keeping the visual language consistent across the design. I'm ready to write out all the component changes.