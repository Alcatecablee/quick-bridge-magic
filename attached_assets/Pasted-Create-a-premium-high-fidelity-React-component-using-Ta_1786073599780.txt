Create a premium, high-fidelity React component using Tailwind CSS that replicates a dark-mode enterprise UI data flow animation inspired by Apple Continuity, Linear, and Raycast. The design must feel like a living operating system feature driven by micro-motion, avoiding flashy consumer gradients or abrupt "teleporting" states.

Implement the following precise art direction specifications:

1. CORE LAYOUT & STRUCTURE:
- Dark mode theme with a solid charcoal black background (#0B0B0C or similar).
- On the left: a minimalist smartphone outline icon. 
- On the right: a desktop computer monitor outline icon.
- Center: A QR code icon acting as the network hub/router.
- Everything connected by a single horizontal SVG data line.
- Device screens must show realistic, abstract UI wireframe structures (blocks, tiny rows, no text) instead of generic placeholders.
- Apply subtle 3D perspective via CSS transforms to the devices (Phone: rotateY(-5deg) rotateX(2deg); Computer: rotateY(5deg) rotateX(1deg)) and add a deep, barely-noticeable blur shadow (blur: 18px, opacity: 8%) to create organic depth.

2. LIVING SYSTEM MICRO-MOTION (THE "BREATH"):
- Idle Motion: Apply a highly subtle, continuous floating idle animation to both devices (rotation between -0.2° and 0.2° over a 16-second loop). It must be nearly imperceptible.
- Screen Breathing: Implement a very slow, subtle OLED screen brightness pulse on the device screens (cycling between 98% and 100% opacity every 8 seconds).

3. LOGICAL DATA PACKET STREAMING:
- The connection line must consist of a base line, a subtle glow layer (pulsing 0% to 15% opacity), and an animated SVG stroke path for traveling data packets.
- Packets must travel logically: Phone -> QR Hub -> Computer.
- Simulate latency: Do not use a rhythmic metronome. Randomize packet departure times (e.g., intervals varying between 1.6s, 2.3s, 1.8s, and 2.9s).
- Device Reaction: When a packet leaves the phone, the phone screen should slightly "lift" or push up by 2px, then drop back. When the packet arrives at the computer, the computer's screen border should briefly glow and settle within a 120-180ms window.

4. EASING CURVES & PREMIUM TIMING:
- Strictly ban default linear or robotic 'easeInOut' interpolations.
- Use premium CSS cubic-bezier easing curves for all animations: `cubic-bezier(0.16, 1, 0.3, 1)` or `cubic-bezier(0.22, 1, 0.36, 1)` for a settling, high-end physical quality.

5. "CONFIDENCE" STATE & CONTINUITY LOGS:
- Below the hub, place a "Connected" badge. 
- The indicator dot next to it must not blink or flash like an alarm. It should be a confident solid green with a soft, slow breathing pulse scale every 4 seconds.
- Instead of static "Connected" text, cycle occasionally through meaningful system events every few seconds to show the devices actively cooperating:
  "✓ Connected" -> "Pasted on Laptop" -> "Tab opened" -> "Photo received" -> "Clipboard synced" -> "✓ Connected"
